apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: backend
spec:
  gateways:
  - kubeflow/kubeflow-gateway
  hosts:
  - '*'
  http:
  - match:
    - uri:
        prefix: /notebooks/api/
    rewrite:
      uri: /api/v1/
    route:
    - destination:
        host: nb-backend.kubeflow-system.svc.cluster.local
        port:
          number: 4000